fix: 冒烟测试改成自带固定数据，不再依赖我这台机器

CI 第一次跑就挂了，暴露了测试本身的问题：两个脚本把
'C:/Users/Nan/.dsh/skills' 和 npx 缓存里的 cordis 路径写死了，
总数据断言成 34。别人 clone 下来 npm test 必然失败——
这正是 CI 该抓的那类 bug，只是这次抓的是测试自己。

- 新增 .smoke-fixtures.mjs：在临时目录里造 4 个技能的 SKILL.md
  （覆盖带双引号、裸值、单引号、disable-model-invocation 四种写法）
  和一份故意漏掉一个技能的备注表
- 宿主端断言改用固定数据：总数 4、自动登记 1 条、二次构建幂等、
  坏 JSON / 文件缺失 / 目录不存在三条降级路径
- 客户端断言改用同一份固定数据：行数、分类胶囊集合、未备注提示
  都按固定数据算，另加一段验证「有 N 个还没备注」的顶部提示
- DSH_HOME 在导入被测模块前指向临时目录（src 在模块初始化时就算好路径），
  结束时恢复原值并删掉临时目录
- cordis 与真实技能目录的查找放宽：找不到就跳过 apply() 那一段并打印说明，
  真实 ~/.dsh/skills 只做只读展示，不参与判定

本机：宿主端 43 项 + 客户端 44 项全绿。
